<h2>Confirm your order</h2>
<p>You can make changes later (if you make an order within 24 hours of the delivery date the changes might be noticed until your next delivery).</p>

<p>
  <b>Address:</b>
  <%= @order.address %>
</p>

<p>
  <b>Postcode:</b>
  <%= @order.postcode %>
</p>

<p>
  <b>Number of loaves:</b>
  <%= @order.number_of_loaves %>
</p>

<p>
	<b>Excluded ingredients:</b>
	<% @order.excluded_ingredients.each do |ingredient| %>
		<%= ingredient.name %> 
		<%= "," unless ingredient == @order.excluded_ingredients.last -%>
	<% end -%>
</p>


<%= link_to 'Any changes?', edit_customer_order_path(@customer, @order) %>
